<!doctype html>
<html lang="en">
  <head>
    {% render "head", metadata: site.metadata, title: page.title, metadescription: page.metadescription %}
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300;600&display=swap" rel="stylesheet">

    <script src='https://kit.fontawesome.com/fc08c5bc44.js' crossorigin='anonymous'></script>

  </head>
  <body class="{{ page.layout }} {{ page.page_class }}">
    <header class="bg-yellow-200">
    {% render "navbar", metadata: site.metadata, page: page %}
      <div class='min-w-100 md:flex justify-center items-center'>
        <img-container class="inline-block md:w-1/3 mx-32 my-8">
          <img src='images/logo-prirodzene.png' >
        </img-container>

        <div class='inline-block md:w-1/2 mx-8 md:ml-0 md:mr-16'>
          <div class='w-50 flex-initial flex'>
            <quote class='flex flex-col'>
              <p class='text-lg md:text-2xl text-center md:text-left max-w-prose'>„Kvalitné sexuálne vzdelávanie vyžaduje, aby sme odložili egá a
              pripustili, že to, čo si myslíme, že vieme, nie je skutočným odzrkadlením reality.“</p>
              <p class="text-right py-8">Emily L. Depasse</p>
            </quote>
          </div>
        </div>
      </div>
    </header>

    <main>
      {{ content }}
    </main>

    {% render "footer", metadata: site.metadata %}
  </body>
</html>
